{
  "name": "ai-note-taker-server",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node app.js",
    "dev": "node --watch app.js",
    "build": "node --check app.js && node --check auth.js && node --check billing.js && node --check email.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "ejs": "^3.1.10",
    "express": "^5.1.0",
    "mongoose": "^8.18.0"
  },
  "engines": {
    "node": "24.x"
  }
}
